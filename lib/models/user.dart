class User {
  String firstName;
  String lastName;
  String address;
  String gender;
  DateTime birthDay;
  String userId;

  User(this.firstName,this.userId, this.lastName, this.address, this.gender, this.birthDay);
}